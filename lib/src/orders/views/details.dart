import 'package:flutter/material.dart';

class OrderView extends StatelessWidget {
  static const routeName = 'orders/:orderId';
  const OrderView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
